# Frontend Environment Variables

# API Configuration
VITE_API_BASE_URL=http://localhost:5000


# Development vs Production
# For development (local backend)
# VITE_API_BASE_URL=http://localhost:5000

# For production (deployed backend)
# VITE_API_BASE_URL=https://your-api-domain.com

# Admin Configuration
# These are handled on the backend, but you can add frontend-specific admin configs here
# VITE_ADMIN_PANEL_ENABLED=true
# VITE_ADMIN_SESSION_TIMEOUT=3600000

# Note: All frontend environment variables must start with VITE_ prefix
# This allows Vite to expose them to the client-side code